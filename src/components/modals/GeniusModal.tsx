import React from 'react';

// This component is intentionally left blank to remove the AI/Gemini dependency
// and ensure full offline functionality.
const GeniusModal: React.FC<{ isOpen: boolean; onClose: () => void; }> = () => {
    return null;
};

export default GeniusModal;
